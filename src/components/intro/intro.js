var $ = require('jquery');
var transitions = require('../../common/transitions');

require('imports?$=jquery!../../vendors/velocity');

angular.module('App.components').component('intro', {
  bindings: {},
  templateUrl: '/templates/intro.html',
  controllerAs: 'intro',
  controller: function($scope) {
    debugger;
  }
});
